/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.scdn.co", "mosaic.scdn.co", "seed-mix-image.spotifycdn.com", "seeded-session-images.scdn.co"],
  },
};

module.exports = nextConfig;
